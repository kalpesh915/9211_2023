/**
    character array / string
    character enclaused in ""
    %s is format character for string
*/

#include<stdio.h>

void main(){
    char ip[100] = {'A', 'N', 'K', 'I', 'T'};
    printf("\n Welcome %s", ip);
}
